from django.urls import path
from . import views

app_name = 'reports'

urlpatterns = [


    # path('', views.ListView.as_view(), name='list'),      # page liste rapports
    # path('generate/', views.GenerateView.as_view(), name='generate'),  # générer rapport

]
