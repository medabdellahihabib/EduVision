from django.urls import path
from . import views

app_name = 'attendance'

urlpatterns = [


    # path('', views.ListView.as_view(), name='list'),      # page liste présences
    # path('create/', views.CreateView.as_view(), name='create'),  # ajouter présence
]
